{
    "private": true,
    "scripts": {
        "init": "docker run --rm --interactive --tty -v $(pwd):/app composer install",
        "build": "./vendor/bin/sail build",
        "up": "./vendor/bin/sail up",
        "migrate": " ./vendor/bin/sail artisan migrate:fresh --seed"
    },
    "devDependencies": {
        "axios": "^0.21",
        "laravel-mix": "^6.0.6",
        "lodash": "^4.17.19",
        "postcss": "^8.1.14"
    }
}
